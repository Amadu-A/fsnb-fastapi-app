// /static/js/app.js
// Минимальный файл для будущих взаимодействий на фронте
window.addEventListener("DOMContentLoaded", () => {
  // Пример "живости": лог отметки загрузки
  console.debug("[fsnb-fastapi] dom-ready");
});
